# ============================================================
# .env — Environment variables (DO NOT commit this file!)
# Copy this to .env and fill in your actual values
# ============================================================

# Your MongoDB Atlas connection string
# Get it from: Atlas Dashboard > Connect > Drivers > Copy the URI
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/ultangber?retryWrites=true&w=majority

# Port the server runs on (Render sets this automatically)
PORT=3000
